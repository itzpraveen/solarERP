{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && cd client-new && npm install && npm run build && cd .. && npx sequelize-cli db:migrate --env production"
  },
  "deploy": {
    "startCommand": "npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30
  },
  "environments": {
    "production": {
      "build": {
        "buildCommand": "npm install && cd client-new && npm install && npm run build && cd .. && npx sequelize-cli db:migrate --env production && npm run create-admin || true"
      }
    }
  }
}